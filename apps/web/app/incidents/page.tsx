'use client';
import { useEffect, useState } from 'react'; import { AppShell } from '../../components/app-shell'; import { StatusBadge } from '../../components/status-badge'; import { api } from '../../lib/api';
type Incident={id:string;status:string;reason:string;startedAt:string;resolvedAt:string|null;monitor:{name:string;url:string}};
export default function IncidentsPage(){const [incidents,setIncidents]=useState<Incident[]>([]);const [error,setError]=useState('');useEffect(()=>{api<Incident[]>('/incidents?limit=100').then(setIncidents).catch(()=>setError('Unable to load incidents.'));},[]);return <AppShell title="Incidents"><div className="page"><div className="page-title"><div><h2>Incidents</h2><p>Downtime events detected after consecutive failed checks.</p></div></div>{error&&<div className="notice">{error}</div>}<section className="surface"><div className="table-wrap"><table><thead><tr><th>Monitor</th><th>Status</th><th>Reason</th><th>Started</th><th>Resolved</th></tr></thead><tbody>{incidents.length?incidents.map(i=><tr key={i.id}><td><strong>{i.monitor.name}</strong></td><td><StatusBadge status={i.status}/></td><td>{i.reason}</td><td>{new Date(i.startedAt).toLocaleString()}</td><td>{i.resolvedAt?new Date(i.resolvedAt).toLocaleString():'—'}</td></tr>):<tr><td colSpan={5} className="empty">No incidents have been detected.</td></tr>}</tbody></table></div></section></div></AppShell>}
