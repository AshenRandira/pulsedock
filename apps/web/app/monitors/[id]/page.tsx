'use client';
import { Play, RefreshCw } from 'lucide-react'; import { useParams } from 'next/navigation'; import { useCallback, useEffect, useState } from 'react'; import { AppShell } from '../../../components/app-shell'; import { StatusBadge } from '../../../components/status-badge'; import { api } from '../../../lib/api';
type Monitor = { id:string; name:string; url:string; currentStatus:string; intervalMinutes:number; expectedStatusCode:number; lastCheckedAt:string|null }; type Check = { id:string; success:boolean; statusCode:number|null; responseTimeMs:number|null; checkedAt:string; errorMessage:string|null }; type Incident = { id:string; status:string; reason:string; startedAt:string; resolvedAt:string|null };
export default function MonitorDetailPage() { const { id } = useParams<{id:string}>(); const [monitor,setMonitor]=useState<Monitor>(); const [checks,setChecks]=useState<Check[]>([]); const [incidents,setIncidents]=useState<Incident[]>([]); const [error,setError]=useState(''); const [checking,setChecking]=useState(false); const load=useCallback(()=>Promise.all([api<Monitor>(`/monitors/${id}`),api<Check[]>(`/monitors/${id}/check-results?limit=20`),api<Incident[]>(`/monitors/${id}/incidents?limit=20`)]).then(([m,c,i])=>{setMonitor(m);setChecks(c);setIncidents(i);}).catch(()=>setError('Unable to load this monitor.')),[id]); useEffect(()=>{void load();},[load]); async function check(){setChecking(true);try{await api(`/monitors/${id}/check`,{method:'POST'});await load();}catch{setError('Manual check failed to start.');}finally{setChecking(false);}} return <AppShell title="Monitor detail"><div className="page">{error&&<div className="notice">{error}</div>}<section className="surface monitor-hero"><div><h3>{monitor?.name ?? 'Loading monitor…'}</h3><p>{monitor?.url}</p><p>Expected {monitor?.expectedStatusCode} · every {monitor?.intervalMinutes} minutes</p></div><div><button className="button" onClick={check} disabled={checking}><Play size={16}/>{checking?'Checking…':'Run check'}</button></div></section><div className="two-col"><section className="surface"><div className="surface-head"><h3>Check history</h3><button className="button secondary" onClick={load}><RefreshCw size={15}/> Refresh</button></div><div className="table-wrap"><table><thead><tr><th>Time</th><th>Result</th><th>HTTP</th><th>Response</th></tr></thead><tbody>{checks.length?checks.map(c=><tr key={c.id}><td>{new Date(c.checkedAt).toLocaleString()}</td><td><StatusBadge status={c.success?'UP':'DOWN'}/></td><td>{c.statusCode??'—'}</td><td>{c.responseTimeMs??'—'} ms</td></tr>):<tr><td colSpan={4} className="empty">No checks recorded yet.</td></tr>}</tbody></table></div></section><section className="surface"><div className="surface-head"><h3>Incidents</h3></div><div className="table-wrap"><table><thead><tr><th>Status</th><th>Started</th></tr></thead><tbody>{incidents.length?incidents.map(i=><tr key={i.id}><td><StatusBadge status={i.status}/></td><td title={i.reason}>{new Date(i.startedAt).toLocaleString()}</td></tr>):<tr><td colSpan={2} className="empty">No incidents for this monitor.</td></tr>}</tbody></table></div></section></div></div></AppShell>; }
