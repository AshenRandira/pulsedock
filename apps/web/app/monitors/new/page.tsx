'use client';
import { useRouter } from 'next/navigation'; import { FormEvent, useState } from 'react'; import { AppShell } from '../../../components/app-shell'; import { api } from '../../../lib/api';
export default function NewMonitorPage() { const router = useRouter(); const [error, setError] = useState(''); const [saving, setSaving] = useState(false); async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); setSaving(true); setError(''); const form = new FormData(event.currentTarget); try { await api('/monitors', { method:'POST', body: JSON.stringify({ name: form.get('name'), url: form.get('url'), intervalMinutes: Number(form.get('interval')), expectedStatusCode: Number(form.get('status')), isActive: form.get('active') === 'on', isPublic: form.get('public') === 'on' }) }); router.push('/monitors'); } catch { setError('Monitor could not be created. Check the URL and API connection.'); } finally { setSaving(false); } } return <AppShell title="New monitor"><div className="page"><div className="page-title"><div><h2>Add monitor</h2><p>Set the endpoint and the response PulseDock should expect.</p></div></div>{error && <div className="notice">{error}</div>}<form className="surface form" onSubmit={submit}><div className="field"><label htmlFor="name">Name</label><input id="name" name="name" required placeholder="Production API" /></div><div className="field"><label htmlFor="url">URL</label><input id="url" name="url" type="url" required placeholder="https://api.example.com/health" /></div><div className="form-row"><div className="field"><label htmlFor="interval">Check interval (minutes)</label><input id="interval" name="interval" type="number" min="1" max="1440" defaultValue="5" /></div><div className="field"><label htmlFor="status">Expected HTTP status</label><input id="status" name="status" type="number" min="100" max="599" defaultValue="200" /></div></div><div className="checks"><label><input name="active" type="checkbox" defaultChecked /> Active</label><label><input name="public" type="checkbox" /> Show on public status</label></div><div><button className="button" disabled={saving}>{saving ? 'Creating…' : 'Create monitor'}</button></div></form></div></AppShell>; }
