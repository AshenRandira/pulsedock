'use client';
import Link from 'next/link'; import { Plus } from 'lucide-react'; import { useEffect, useState } from 'react';
import { AppShell } from '../../components/app-shell'; import { StatusBadge } from '../../components/status-badge'; import { api } from '../../lib/api';
type Monitor = { id: string; name: string; url: string; currentStatus: string; intervalMinutes: number; isActive: boolean; lastCheckedAt: string | null };
export default function MonitorsPage() { const [monitors, setMonitors] = useState<Monitor[]>([]); const [error, setError] = useState(''); useEffect(() => { api<Monitor[]>('/monitors').then(setMonitors).catch(() => setError('Unable to load monitors.')); }, []); return <AppShell title="Monitors"><div className="page"><div className="page-title"><div><h2>Monitors</h2><p>Endpoints currently tracked by this PulseDock instance.</p></div><Link className="button" href="/monitors/new"><Plus size={17} /> New monitor</Link></div>{error && <div className="notice">{error}</div>}<section className="surface"><div className="table-wrap"><table><thead><tr><th>Name</th><th>URL</th><th>Status</th><th>Interval</th><th>Last check</th></tr></thead><tbody>{monitors.length ? monitors.map(m => <tr key={m.id}><td><Link href={`/monitors/${m.id}`}><strong>{m.name}</strong></Link></td><td>{m.url}</td><td><StatusBadge status={m.isActive ? m.currentStatus : 'DISABLED'} /></td><td>{m.intervalMinutes} min</td><td>{m.lastCheckedAt ? new Date(m.lastCheckedAt).toLocaleString() : 'Never'}</td></tr>) : <tr><td colSpan={5} className="empty">No monitors yet. Add the first endpoint to begin tracking.</td></tr>}</tbody></table></div></section></div></AppShell>; }
